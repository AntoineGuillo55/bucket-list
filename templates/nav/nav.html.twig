{% block nav %}
    <nav>
        <ul>
            <li><a href="{{ path('main_home') }}">Home</a></li>
            <li><a href="{{ path('main_about') }}">About us</a></li>
            <li><a href="{{ path('wish_list') }}">Wish List</a></li>
            <li><a href="{{ path('events_list') }}">Event list</a></li>
            {% if app.user %}
                <li><a href="{{ path('wish_add') }}">Add</a></li>
                <li><a href="{{ path('app_logout') }}">Logout</a></li>
            {% else %}
                <li><a href="{{ path('app_login') }}">Sign in</a></li>
                <li><a href="{{ path('app_register') }}">Register</a></li>
            {% endif %}
        </ul>
    </nav>
{% endblock %}
